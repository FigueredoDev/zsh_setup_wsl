# ============================================================================
# Variáveis de Ambiente - Template
# Copie este arquivo para .env e configure suas variáveis sensíveis
# ============================================================================

# Sourcegraph Cody (para funções de commit automático)
SRC_ENDPOINT=https://sourcegraph.com/
SRC_ACCESS_TOKEN=seu_token_aqui

# GPG (configuração automática)
GPG_TTY=$(tty)

# Diretórios customizados (opcional)
# GOPATH=/caminho/personalizado/go
# PYENV_ROOT=/caminho/personalizado/.pyenv
# NVM_DIR=/caminho/personalizado/.nvm

# Configurações de desenvolvimento (descomente apenas se quiser versões específicas)
# Por padrão, instala sempre as versões LTS/mais recentes estáveis
# DEFAULT_NODE_VERSION=18              # Se não definido, usa LTS
# DEFAULT_PYTHON_VERSION=3.11.0        # Se não definido, usa a mais recente estável
# DEFAULT_JAVA_VERSION=17.0.8-amzn     # Se não definido, usa LTS
# DEFAULT_GO_VERSION=1.21.0            # Se não definido, usa a mais recente

# Configurações do ZSH
ZSH_THEME=spaceship
# ZSH_THEME=dracula/dracula  # alternativa

# Git (configuração global)
GIT_USER_NAME="Seu Nome"
GIT_USER_EMAIL="seu.email@exemplo.com"

# Configurações opcionais
INSTALL_OPTIONAL_TOOLS=true  # Console Ninja, Gemini CLI, etc.
SKIP_OH_MY_ZSH_UPDATE=false
ENABLE_AUTO_UPDATE=true

# Proxy (se necessário)
# HTTP_PROXY=http://proxy.empresa.com:8080
# HTTPS_PROXY=http://proxy.empresa.com:8080
